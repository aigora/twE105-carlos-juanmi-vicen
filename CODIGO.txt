#include <stdio.h>
#include <math.h>
#include <windows.h>

void escribeTexto(char cadena[]); //funcion para que las letras vayan de una en una

int main() //programa principal
{
	char nombre[50];
	int i=0;
	printf("         ---------------------------------------------\n         ---------------------------------------------\n");
	printf("         ********BIENVENIDO A ATRAPA UN MILLON********\n         ---------------------------------------------\n");
	printf("         ---------------------------------------------\n         ---------------------------------------------\n");
	char texto1[]= "Hola!, soy Carlos y te doy la bienvenida a ATRAPA UN MILLON.\n\
Para empezar, me gustaria saber tu nombre para conocernos mejor:\n";
	escribeTexto(texto1);
	gets(nombre);
}

void escribeTexto(char cadena[]) //funcion letras de una en una
{
	int i=0;
	while(cadena[i]!='\0')
	{
		printf("%c", cadena[i]);
		i++;
		Sleep(30);
	}
}
